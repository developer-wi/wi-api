[project]
name = "auth-v1"
version = "0.1.0"
description = ""
authors = [
    { name = "Wonik Kim", email = "developer.wi.76@gmail.com" }
]
readme = "README.md"


[tool.poetry]
packages = [{ include = "module", from = "src" },{ include = "auth", from = "src" },{ include = "util", from = "src" }]

[tool.poetry.dependencies]
python = "^3.10"
passlib = { extras = ["bcrypt"], version = "^1.7.4" }
pyjwt = "^2.10.1"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
