[project]
name = "blog-v1"
version = "0.1.0"
description = ""
authors = [
    { name = "Wonik Kim", email = "developer.wi.76@gmail.com" }
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
]

[tool.poetry]
packages = [{ include = "blog", from = "../../api-v1" }]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
