[project]
name = "server"
version = "0.1.0"
description = ""
authors = [
    {name = "wi-dev",email = "developer.wi.76@gmail.com"}
]

requires-python = ">=3.13"


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.dependencies]
fastapi = "^0.115.8"
uvicorn = "^0.34.0"
